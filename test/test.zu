@module main;

const main := fn () int {
    have x: int = 5;
    if (x < 3) return 1;
    if (x > 10 ) {
        @outputln(1, "x: ", x, ", and exiting with code: 2");
        return 2;
    } else {
        @outputln(1, "x: ", x, ", and exiting with code: 3");
        return 3;
    }
};
