@module main;

const Kind := enum {
    None,
    Int,
    Float,
    String,
    Bool
};

# TODO: Add in Structs, and member expressions to access them
# NOTE: Maybe something like Kind::None for non initialized variables
# NOTE: and maybe x.None for initialized variables

const main := fn () int {
};
