@module main;

const main := fn () int {
    have i: int = 0;
    loop (i < 4) : (i++) {
        @outputln(1, "i: ", i);
        have j: int = 0;
        loop (j <= i) : (j++) {
            @outputln(1, "j: ", j);
        }
    }
};
